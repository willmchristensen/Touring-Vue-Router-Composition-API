<script setup>
const props = defineProps(['event']);
</script>

<template>
  <div v-if="event">
    <h1>{{ event.title }}</h1>
    <!-- LAYOUT COMPONENT REPLACES NAVIGATION -->
    <p>{{ event.time }} on {{ event.date }} @ {{ event.location }}</p>
    <p>{{ event.description }}</p>
  </div>
</template>
